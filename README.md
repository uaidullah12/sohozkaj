# SohozKaj

This repository contains the SohozKaj web app (Bengali photo & document toolkit) scaffold.

Dev setup:

1. Copy .env.example to .env and fill DATABASE_URL and JWT_SECRET.
2. npm install
3. npx prisma generate
4. npx prisma migrate dev --name init
5. npm run seed
6. npm run dev

